apply plugin: "com.android.application"
apply plugin: "com.facebook.react" // Этой строки достаточно, если пути в allprojects верны
apply plugin: "org.jetbrains.kotlin.android"

// ... остальной код файла
